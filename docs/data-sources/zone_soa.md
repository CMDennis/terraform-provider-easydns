---
page_title: "easydns_zone_soa Data Source - EasyDNS"
description: |-
  Fetches the current SOA serial for an EasyDNS zone.
---


# easydns_zone_soa (Data Source)

~> **Unverified; every live read failed.** Against the EasyDNS sandbox this endpoint answered `HTTP 400: timeout on read select()` for every domain tried, on three consecutive attempts. That looks like a backend timeout rather than a rejection, and it may behave correctly in production; see the [verification status guide](../guides/verification-status).


```terraform
data "easydns_zone_soa" "current" {
  domain = "example.com"
}

output "soa_serial" {
  value = data.easydns_zone_soa.current.serial
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `domain` (String) Domain whose SOA serial is returned.

### Read-Only

- `serial` (Number) Current SOA serial.
